// App imports
import { MapContainer } from './container';
import { MapElements } from './elements';

export const Maps = () => {
	return (
		<>
			<MapContainer/>
			<MapElements/>
		</>
	)
}

Maps.displayName="Maps";