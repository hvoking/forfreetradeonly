// App imports
import { Wrapper } from './wrapper';
import { MapContainer } from './maps';

export const Main = () => {
  return (
  <Wrapper>
    <MapContainer/>
  </Wrapper>
  );
};

Main.displayName = "Main";